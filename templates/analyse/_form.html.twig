{{ form_start(form, {'attr': {'class': 'your-form-class w-75 mx-auto'}}) }}
    <div class="form-group">
        {{ form_label(form.nom) }}
        {{ form_widget(form.nom, {'attr': {'class': 'form-control', 'placeholder': 'Name'}}) }}
    </div>
    <div class="form-group">
        {{ form_label(form.duree) }}
        {{ form_widget(form.duree, {'attr': {'class': 'form-control', 'placeholder': 'LastName'}}) }}
    </div>
    <div class="form-group">
        {{ form_label(form.prerequis) }}
        {{ form_widget(form.prerequis, {'attr': {'class': 'form-control', 'placeholder': 'Birthday'}}) }}
    </div>
    <div class="form-group">
        {{ form_label(form.contenu) }}
        {{ form_widget(form.contenu, {'attr': {'class': 'form-control', 'placeholder': 'Adresse'}}) }}
    </div>

    <div class="d-flex justify-content-center m-4">
    <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
    </div>
{{ form_end(form) }}
